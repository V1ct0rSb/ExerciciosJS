// Dado o seguinte objeto:
const pessoa = {
  nome: "João",
  idade: 25,
  cidade: "São Paulo",
};

// Use destructuring para extrair o nome e a idade.
// Em seguida, imprima essas variáveis.

// Seu código aqui

const { nome, idade } = pessoa;

console.log(nome);
console.log(idade);
