// 1. Crie uma função chamada "soma" que recebe dois parâmetros e retorna a soma deles.
// 2. Chame a função com alguns valores e imprima o resultado no console.

// 1
function soma(a, b) {
  return a + b;
}

// 2
var resultado = soma(3, 2);
console.log(resultado);
